import React from 'react'

export default function Upload(){
  return (
    <section className="page upload">
      <h2>Upload Artworks</h2>
      <p>This project uses Netlify CMS for uploads. To upload artworks, use the admin panel at <code>/admin</code> after deploying and enabling Identity + Git Gateway.</p>
    </section>
  )
}
